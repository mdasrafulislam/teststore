package it.unibz.teststore.repository;

import it.unibz.teststore.entity.PreCalculation;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PreCalcuationRepository extends JpaRepository<PreCalculation, Integer>{

	
}
